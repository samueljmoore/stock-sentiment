import React from 'react';
import { Box, Divider, Typography } from '@mui/material';
import Header from './Header';

const App = () => {
  return (
    <Box>
      <Header />
    </Box>
  );
};

export default App;
